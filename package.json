{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "NFC card portfolio — Layer 1",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "node --watch server.js",
    "stop":    "pkill -f 'node.*server.js' && echo '⏹  Server stopped.' || echo '⚠  No server running.'",
    "reindex": "node scripts/reindex.js",
    "scrape-all": "node scripts/scrape-all.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "async-mutex": "^0.5.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.0",
    "helmet": "^8.3.0",
    "playwright": "^1.62.0",
    "vectra": "^0.15.0"
  }
}
